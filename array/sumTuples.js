/**
 * Find all the unique pair from given array, which sums
 * up to make passed value
 * 
 * @param {array} arr - problem array
 * @param {number} sum - sum to match
 * 
 * @param {array}
 */
function findSumTuples(arr, sum) {
  
}
